PNG INSPECTOR TOOL

inspect png images because there is nothin else better to do 

Commands: 

        --idhr:
        Inspect IDHR properties 

        --chunk:
        Inspect the chunks

        --hexdump:
        Dump the raw binary data

        --crc
        Verify CRC of chunks
